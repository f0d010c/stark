interface:
  display_name: "Stark Codex"
  short_description: "Opinionated anti-slop UI/UX design routing"
  default_prompt: "Use $stark-codex to design, improve, or audit this UI/UX so it does not feel generic or hard to use."

policy:
  allow_implicit_invocation: true
