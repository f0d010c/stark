# UI pattern briefs

These briefs define visual and interaction quality, not product flow. Use them after the UX decision brief, or directly when the user asks mainly for visual design.

Good UI decisions are contextual:

- A CRM dashboard should be dense, stable, and scannable.
- A marketing page should be memorable, directed, and conversion-aware.
- A native settings screen should feel boring in the right way.
- An editor should protect the canvas and make controls discoverable.
- A desktop app should choose a real app archetype before reaching for the same sidebar, cards, and settings shell.
- An original app/page should choose a subject-specific concept, composition archetype, one weird move, and restraints before styling.
- A cinematic animated page should storyboard the main object, transformation, timeline beats, camera, assets, libraries, and fallback before code.

Use these briefs to choose visual density, hierarchy, component grammar, motion budget, type, materials, imagery, and state treatment before writing code.

For serious UI generation, read `design-preflight.md` first so Stark chooses the prompt type, must-read/skipped guidance, effect caps, evidence, and stop conditions before loading the larger design system. Then read `design-execution-protocol.md` as the order of operations. It connects surface classification, product object, pattern, visual system, choreography, component anatomy, implementation containment, and browser QA.

When a prompt could reasonably become several different outputs, read `design-mode-router.md` after preflight and before product intent, surface playbooks, libraries, or visual style. Use it to choose targeted repair, product app, product proof landing, cinematic animation, trust/risk flow, docs platform, visual system, or gallery proof, plus quality target, interaction depth, expression/library/motion ceilings, evidence, and hard no.

For broad "go all out" or design-director work across research, originality, patterns, typography, copy, assets, libraries, usability, responsive strategy, and QA, read `design-director-runbook.md` before implementation. It wraps the references into one ordered decision artifact and report.

When a request implies "next level", public proof, portfolio quality, stronger screenshots, better generation quality, or serious product UI, read `ambition-calibration-contract.md` after preflight and before capability, motion, asset, or typography selection. Use it to set functional/good/polished/high-craft/portfolio target, evidence depth, what to maximize, what to keep quiet, rejected spectacle, and stop/continue conditions.

After ambition and surface are known, read `ambition-surface-recipes.md` before implementation. Use it to choose the concrete surface recipe, maximized dimensions, quiet support, structure, typography, motion, library ceiling, asset/proof strategy, usability proof, QA evidence, rejected effort, and stop/continue condition.

When broad, premium, animation-heavy, library-heavy, or high-craft work risks becoming overdesigned, read `taste-calibration-system.md` before choosing libraries, assets, or effects. Use it to set expression level, density, novelty budget, motion budget, library budget, restraints, one allowed showpiece, removals, and QA taste checks.

When original, high-craft, next-level, public, portfolio, animation-led, repeated-generation, or "generate a couple designs" work is requested, read `variant-selection-loop.md` before implementation. Use it to create meaningfully different directions, score product specificity, structure, proof, typography, motion, libraries, usability, responsive strategy, originality, and implementation risk, then select one direction and reject the rest.

When research or inspiration is requested, read `reference-board-workflow.md` before implementation. Use it to compare 3-5 references, extract structural/motion/state/responsive lessons, and name what not to copy. Then read `research-synthesis-contract.md` so the research becomes binding layout, component, library, typography, choreography, usability, responsive, and QA decisions.

When a generated design needs to match or exceed a real reference without copying it, read `benchmark-repair-loop.md` after the reference board. Use it to name the quality gap, originality boundary, one high-impact repair, before/after evidence, and remaining delta.

For animated heroes, scroll films, product reveals, Luma-style references, or key-art scenes that feel messy or pasted together, read `animation-composition-cohesion-gate.md` after premium animation direction and art direction. Use it to accept, repair, or block the product idea, hero object, scene container, asset job map, depth/perspective, lighting/material system, text integration, motion job, originality boundary, and re-check evidence.

For web implementation, read `web-implementation-tracks.md`, then `stack-blueprints.md` after the track is chosen. Before adding libraries or advanced effects, read `frontend-technique-matrix.md` so native browser primitives, library owners, fallbacks, containment, performance, accessibility, and QA probes are deliberate. For app-like or stateful work, read `frontend-component-standards.md` before code so the implementation has clear shell, feature, primitive, state, keyboard, responsive, and QA ownership.

For serious, library-heavy, animation-led, app-like, or high-craft web generation, read `generation-blueprint-contract.md` after surface, stack, technique, and library decisions. Use it to convert the brief into exact files/components, product data, states, library owners, choreography hooks, assets, type/tokens, responsive replacements, accessibility/performance hooks, QA hooks, and deleted defaults before code.

After the generation blueprint, read `implementation-pattern-library.md` when the surface matches product proof, operations, agent runs, editors, trust/permissions, docs, animated product stories, or data proof. Use the pattern's file shape, state model, library ownership, responsive replacement, QA hooks, and removal list as the implementation scaffold.

When a high-craft or product-proof generation still needs a memorable but usable starting shape, read `signature-surface-recipes.md` after `implementation-pattern-library.md`. Use it to choose proof workbench landing, cinematic output reveal, operational cockpit, agent run review, editor instrument, trust decision flow, docs command manual, data proof panel, gallery proof system, or mobile task stack with a matching product object, proof surface, typography posture, library preset, states, responsive replacement, originality move, and QA evidence.

Immediately before serious code generation, read `prompt-to-build-compiler.md`. Use it to turn the blueprint and pattern into an implementation directive with component inventory, state fixtures, library owners, type/tokens, choreography hooks, QA selectors, forbidden defaults, and acceptance comparison.

When a web surface may combine native primitives, product libraries, motion libraries, visual libraries, data-viz, forms, media, or generated assets, read `library-orchestration-system.md` after selecting candidates and before implementation. Use it to name one owner per behavior, QA probes, rejected libraries, and removal rules.

When the user asks to maximize libraries/tools, use Motion/GSAP/Three/Rive/Lottie/View Transitions/product libraries, or make a result feel like a real designer/frontend developer built it, read `capability-maximization-matrix.md` before library orchestration. Then read `library-capability-fit-contract.md` and `capability-recipe-cookbook.md` so each dependency has a scored job, native alternative, surface recipe, QA probe, and removal trigger before it can join the stack. After a dependency is accepted, read `frontend-library-production-presets.md`, `library-implementation-blueprints.md`, and `library-source-verification-gate.md` so Motion, GSAP, View Transitions, Three/R3F, Rive, Lottie, Anime.js, Theatre.js, PixiJS/canvas, browser APIs, and data-viz choices become source-verified production presets with import boundaries, cleanup, fallbacks, budgets, QA selectors, and removal triggers. After rendered evidence exists, read `capability-stack-rendered-gate.md` to accept, repair, or block the actual stack by product proof gained, usability gained, visual quality gained, accessibility/fallback, mobile/reduced-motion, runtime/performance, library residue, conflicts, and removal evidence.

After implementing serious, public, animation-led, or user-critiqued UI, read `implementation-review-loop.md` before acceptance. Use it to compare source decisions against inspected files/components and rendered evidence, find drift in states, libraries, typography, motion, responsive behavior, usability, accessibility, and performance, then repair and re-check the highest-impact mismatch.

For README/gallery screenshots, public demos, generated design batches, animation pages, component galleries, or high-craft UI that may be regenerated, read `runtime-evidence-regression-contract.md` after browser QA and rendered quality. Use it to define baseline viewports, states, interactions, screenshot scope, masked dynamic regions, runtime/accessibility/performance/media probes, acceptance threshold, update-baseline rule, failure triage, and stored artifacts.

When the UI should survive release, CI, repeated dogfood runs, or public comparison, read `frontend-quality-automation-gate.md` after the evidence/regression contracts. Use it to choose advisory/blocking/release-blocking gate level, local/CI commands, Playwright projects, Storybook/local-gallery coverage, visual baselines, interaction/a11y/performance/usability checks, deterministic fixtures, artifacts, failure triage, and baseline-update rules.

When a web app needs product libraries beyond motion, read `product-library-playbooks.md` before installing or composing them. Use it to assign behavior to Radix, React Aria, shadcn/ui, TanStack Table/Query, React Hook Form, Zod, Floating UI, cmdk, Embla, and rejected options.

After selecting product libraries, read `product-library-composition.md` before implementation. Use it to define behavior/styling/state ownership, token/theme mapping, component anatomy, non-happy states, rejected defaults, and QA so the UI does not look like library demo parts pasted together.

When product libraries are visible in the UI, read `library-de-defaulting-system.md` after `product-library-composition.md`. Use it to identify starter-kit fingerprints, replace tokens/anatomy/states/copy/data/responsive behavior, remove unnecessary components, compare against official docs examples, and verify the product is recognized before the library.

When dashboards, analytics, KPI cards, metric panels, monitoring boards, reports, or data-proof screenshots matter, read `dashboard-insight-hierarchy.md` before chart-library selection. Use it to define audience, monitoring job, primary decision/action, north-star metric, supporting metrics, exception logic, filters, freshness/source, insight order, drill path, non-happy data states, responsive replacement, data honesty, and QA evidence.

When charts, analytics, maps, timelines, metric panels, or data proof matter, read `data-visualization-library-selection.md` before implementation. Use it to name the user question, decision/action, dataset shape, chart form, library, accessible summary, source/table fallback, responsive replacement, motion rule, rejected libraries, and QA checks.

When app shells, multi-route sites, docs platforms, dashboards, admin/settings/workspaces, command/search, breadcrumbs, tabs, or route transitions matter, read `navigation-information-architecture.md` before implementation. Use it to name the primary objects, route map, navigation model, current-location model, search/command behavior, breadcrumb/tab behavior, deep-link state, route states, mobile replacement, focus/scroll restoration, and QA checks.

When the UI includes custom interactive patterns, read `accessibility-interaction-contract.md` before implementation. Use it to define keyboard behavior, focus movement and return, labels/descriptions, live-region/status updates, hidden/inert content, reduced motion, target/touch behavior, rejected patterns, and QA probes. Before acceptance on serious or public UI, read `accessibility-acceptance-gate.md` to decide blocked, repair, acceptable-with-risk, or pass from semantic structure, names/roles/values, keyboard/focus, contrast, reflow, motion, target/input, forms/errors/status, APG behavior, automated checks, manual checks, and re-check evidence. When the UI uses drag, resize, scrub, swipe, shortcuts, command palettes, split panes, canvas/editor controls, carousels, timelines, route/detail transitions, or scroll-driven proof, also read `input-interaction-model-contract.md`, `interaction-physics-direct-manipulation.md`, and `transition-continuity-contract.md` to define input modes, manipulated object, affordances, bounds/constraints, snap points, persistent object, continuity pattern, keyboard/touch path, focus/scroll ownership, reduced-motion/mobile substitutions, rejected interactions, and QA probes.

When the UI includes forms, settings, checkout, onboarding, signup, admin configuration, filters, generation prompts, or any user input that can fail, read `form-state-validation-system.md` before implementation. Use it to define field anatomy, validation timing, dirty/submitting/server states, preservation, recovery, review/confirmation, schema/library ownership, responsive behavior, and QA.

For Windows, macOS, Tauri, Electron, Avalonia, Uno, and other desktop work, read `desktop-app-archetypes.md` before platform components. Choose the app shape first: command center, library, workbench, monitoring cockpit, tray/menu-bar utility, media/consumer, document/knowledge, or setup/preferences. Then read `native-desktop-app-quality-contract.md` so the app defines a real window model, command model, native controls, resize behavior, states, keyboard/focus paths, and platform evidence instead of becoming a web dashboard inside a window.

For original, distinctive, creative, memorable, high-craft, or non-generic work, read `originality-engine.md` before `creative-direction.md`. Choose the concept seed and composition first; then choose typography, materials, motion, and platform components.

For repeated generations or open-ended visual direction, read `style-diversity-matrix.md` after `creative-direction.md` and before `material-systems.md`. Use it to choose a style lane, layout grammar, material strategy, typography posture, color/token strategy, motion posture, memorable anchor, rejected lanes, and anti-sameness bans.

For product-specific visual taste, read `visual-language-decision-matrix.md` after product intent, surface playbook, taste calibration, and style diversity. Use it to translate risk, frequency, density, audience, and primary object into a chosen visual language, material/type/palette/shape/layout/motion/asset/copy posture, motif, tasteful risk, rejected languages, hard bans, and QA checks.

When a rendered output, repeated batch, gallery candidate, or second attempt may still look like the same Stark template after changing copy/colors, read `design-fingerprint-diversity-gate.md` after style diversity, brand motif, variant selection, or rendered quality. Use it to compare structure, motif, typography, palette/material, asset/proof, interaction/motion, product specificity, same-looking residue, required repair, and re-check evidence.

For subjective critique such as cheap, bland, messy, copied, childish, overdesigned, underdesigned, generic, presentation-like, or same-looking, read `design-taste-diagnostics.md` before repair. Use it to translate the wording into likely cause, taste dimension, preserved decision, first repair, what not to change, re-check evidence, and future memory trigger.

For multi-route, multi-section, multi-state, or app-like products, read `experience-continuity-system.md` after navigation/IA and before implementation. Use it to keep product objects, routes, state language, component anatomy, type, tokens, materials, motion, copy, data fixtures, responsive behavior, accessibility, and QA journeys coherent across screens.

For public pages, app shells, product proof, campaigns, plugin pages, portfolios, or generated showcases that need to feel recognizable instead of merely polished, read `brand-identity-motif-system.md` after style diversity, copy, and tokens. Use it to define recognition target, brand words, motif, shape grammar, color/type roles, icon/illustration stance, motion/material echo, product proof tie, repetition rule, restraint rule, rejected identity moves, and QA checks.

When a UI feels like a presentation, wall of text, overexplained demo, weak navigation, confusing docs, checkout/trust, onboarding, settings, dashboard, or agent run surface, read `progressive-disclosure-information-scent.md` before copy and component polish. Use it to define first/second/deep layers, hidden-until-needed content, scent labels, disclosure controls, mobile disclosure, accessibility structure, measurement signals, and QA checks.

For product copy, hero text, CTAs, proof labels, microcopy, error/recovery text, pricing copy, docs copy, or cheap-sounding text, read `copy-system.md` and `../web-copy-voice.md`. Treat copy as interface behavior, not filler. Before accepting serious copy, read `copy-quality-gate.md` so the actual text is accepted, repaired, or blocked by product nouns, action verbs, proof labels, CTA clarity, recovery/risk copy, banned phrases, mobile readability, and re-check evidence.

For lists, tables, dashboards, charts, cards, timelines, forms, search, docs, agent runs, product screenshots, or data-backed product proof, read `product-data-content-system.md` before implementation. Use it to define domain objects, relationships, realistic records, state distribution, edge cases, units/time/money rules, vocabulary, fixture generation, privacy boundaries, and QA scenarios.

For text-heavy, data-backed, localized, multilingual, long-label, currency/date/unit, RTL, dynamic-copy, or custom-font work, read `text-resilience-localization-system.md` before implementation. Use it to define text sources, locale formatting, expansion risk, wrapping/truncation, plural/grammar, RTL/bidi, font fallback, component stress cases, and QA checks.

For original, branded, high-craft, dense, native, or cross-platform UI, read `typography-systems.md` before choosing fonts. Decide native, branded native, custom web, or mixed strategy; then use `../web-fonts.md` and `../../assets/font-pairs.md` for concrete pairs.

For concrete font job/class decisions, read `font-selection-matrix.md` after `typography-systems.md` and before `type-recipes.md`. Use it to choose display/body/mono classes, concrete stacks, loading plan, variable font usage, numeric behavior, rejected fonts, and QA checks by surface.

For style-lane-specific type choices, read `typography-pairing-lab.md` after `style-diversity-matrix.md` and `font-selection-matrix.md`. Use it to map operational ink, trust ledger, studio workbench, cinematic proof, editorial archive, industrial command, product showroom, playful instrument, civic utility, or experimental poster to a concrete pairing archetype, numeric behavior, loading plan, variable/motion rule, and rejected pairing.

For concrete web type choices, read `type-recipes.md` after `typography-systems.md`. Use it to choose a recipe, display/body/mono roles, loading/import plan, numeric behavior, where personality lives, where type stays quiet, and QA checks.

Before importing web fonts, writing `@font-face`, using `next/font`, or shipping custom type, read `font-implementation-contract.md`. Use it to require delivery mode, exact weights/styles, subset/text range, CSS variables/classes, fallback metric strategy, numeric/OpenType features, preload policy, framework integration, license/privacy boundary, text stress fixtures, and rendered QA evidence.

After rendered typography evidence exists, read `rendered-typography-quality-gate.md` before accepting serious, public, dense, high-craft, or user-critiqued UI. Use it to accept, repair, or block actual type roles, hierarchy texture, line breaks, measure, density fit, numeric behavior, long text stress, mobile/zoom behavior, font loading/fallback behavior, default residue, and re-check evidence.

For higher-level design requests that mention libraries, choreography, QA, typography, usability, or "go all out", read `design-orchestration.md`. Use it as the design-director layer that connects concept, library choice, type, motion, usability, and verification.

When the prompt is ambitious, vague, or reference-led, read `design-orchestration-examples.md` after `design-orchestration.md`. Use the examples to produce specific decisions, not copied layouts.

For high-craft pages, complex apps, or ambiguous layouts, read `pattern-selection.md` before `web-layouts.md` or implementation. Choose the product pattern by user mode, primary object, decision/action, mobile replacement, and state coverage.

For full page/app composition, read `composition-system.md` after pattern selection and before visual styling. Use it to choose the first viewport pattern, focal path, grid grammar, section rhythm, proof placement, repeated motif, responsive replacement, and rejected composition.

When a layout feels loose, cheap, misaligned, crowded, over-spaced, fragile, or slightly generated, read `layout-precision-system.md` after composition and before visual tokens. Use it to define grid/container, pane model, alignment spine, spacing rhythm, text measure, media/object frame, hit-target rule, adaptive breakpoints, overflow/reflow plan, optical balance, and QA checks.

When a page/app feels like a slide deck, loose mockup, one-gap template, or cramped tool, read `spatial-rhythm-density-system.md` after layout precision. Use it to define density mode, relationship spacing, repetition rhythm, whitespace job, compression/expansion rules, touch targets, scroll ownership, zoom/reflow checks, and hard fails.

When the structure is right but the rendered result still feels cheap, flat, pasted together, or not screenshot-worthy, read `visual-finish-system.md` after layout precision, typography, and tokens and before rendered quality. Use it for optical alignment, surface detail, value contrast, depth, deletion, motion still frames, and screenshot re-checks.

When desktop, tablet, and mobile need different structures, read `adaptive-composition-system.md` after layout precision and responsive containment. Use it to preserve the core product idea while changing navigation, proof/media, dense regions, motion/assets, text/CTA placement, and QA viewports instead of shrinking desktop. After rendered evidence, read `responsive-adaptation-gate.md` so desktop, tablet, mobile, reflow, touch/input, media crops, dense regions, and scroll ownership are accepted, repaired, or blocked with evidence.

For full pages where the hero is stronger than the rest, or where downstream sections risk becoming generic feature blocks, read `page-proof-architecture.md` after composition and before implementation. Use it to assign section jobs, build a proof ladder, choose a sequence, delete weak sections, preserve mobile proof order, and QA what the second and third sections prove.

For landing pages, product pages, docs homes, pricing, install pages, waitlists, plugin trust pages, campaign pages, or CTA-driven pages, read `conversion-proof-system.md` after page proof and before copy/visual styling. Use it to define visitor intent, source/message match, offer, primary and secondary actions, decision stage, main objection, proof ladder, trust/risk signals, CTA rhythm, friction removals, measurement/events, and QA.

When the product category or user job is clear, read `product-intent-matrix.md` before choosing patterns and product libraries. It maps developer tools, AI generation, dashboards, CRM, checkout, docs, editors, agent runs, permissions, analytics, marketplaces, and campaigns to the right pattern, libraries, motion level, typography posture, states, responsive replacement, and QA evidence.

After product intent is known, read `surface-playbook-matrix.md` before implementation. Use it to choose must-read references, usually-skipped references, structural pattern, product libraries, motion/visual libraries, typography posture, required states, responsive replacement, QA gates, library ceilings, and hard bans by surface.

After the surface playbook and stack choices are known, require `generation-blueprint-contract.md` for serious implementation so selected references and libraries turn into executable structure instead of staying as design intent.

When a known implementation pattern exists, require `implementation-pattern-library.md` before code so the generator starts from a proven product-specific scaffold instead of a generic app/page shell.

For complex product surfaces, read `component-anatomy.md` after pattern selection. Require anatomy-backed parts, states, keyboard/mouse behavior, responsive behavior, and visual traps for command palettes, run timelines, inspectors, trust matrices, pricing, editors, evidence bays, collection walls, and data panels. When a component is reusable, library-backed, stateful, design-system-like, or expected to survive beyond one screenshot, follow with `component-api-variant-contract.md` so public props, controlled/uncontrolled state, slots, variants, density, state hooks, token hooks, accessibility, responsive behavior, library wrapping, story args, selectors, and rejected APIs are explicit. Then read `component-visual-composition-contract.md` before implementation so component surface roles, edge roles, padding, type/icon alignment, states, focus, elevation, repeated-item behavior, and mobile/touch adaptation are owned rather than default library styling.

For serious interactive surfaces, read `interaction-state-matrix.md` after component anatomy. Use it to define empty/loading/partial/stale/permission/disabled/validation/server-error/success/selected/busy/long-running/destructive states, available controls, disabled reasons, recovery paths, focus behavior, mobile behavior, and evidence tasks.

For non-trivial input models, read `input-interaction-model-contract.md` after interaction states and before prototype implementation. Use it to decide click/tap, command, drag, resize, scrub, swipe, scroll, hover, shortcut, canvas pan/zoom, keyboard, and touch paths so the interface is not mouse-only, hover-only, gesture-only, or animation-only. When the UI uses direct manipulation, follow with `interaction-physics-direct-manipulation.md` so handles, bounds, constraints, snap, preview/commit, undo, keyboard/touch alternatives, and performance/accessibility boundaries are explicit. When the UI changes route, detail level, selected object, artifact, modal, data/status, or layout, follow with `transition-continuity-contract.md` so the persistent object, transition job, focus/scroll behavior, recovery behavior, reduced-motion fallback, mobile replacement, and owner are explicit before adding motion.

When a generated page/app shows controls, filters, forms, drawers, command surfaces, inspectors, charts, media controls, or workflow actions, read `interactive-prototype-contract.md` before implementation. Use it to define the primary interaction, state owner, fixture model, working controls, intentionally static controls, state transitions, recovery path, keyboard/touch path, and QA probes so the result is not a static mockup.

For reusable, stateful, library-backed, form/table, dashboard, editor, or high-craft components, read `component-state-gallery.md` before acceptance. Use it to define visible story/state targets, fixture variants, interaction stories, responsive widths, theme/density modes, accessibility probes, and visual regression targets.

When serious components or page sections need proof beyond one screenshot, read `story-driven-qa-harness.md` after the component state gallery. Use it to define Storybook/local-gallery/Playwright story coverage, interaction stories, visual baseline stories, accessibility stories, deterministic fixtures, dynamic-region stabilization, selectors, commands, repair, re-check, and baseline/update decisions.

When reusable components, design-system-like work, multi-theme products, or design-to-code handoff matter, read `design-system-production-loop.md` after component API/state gallery and before final QA. Use it to define source of truth, token source, component inventory, component-to-code mapping, variants/properties, stories/docs, usage guidance, token/code export, drift checks, versioning, exceptions, and ship decision.

For hover, focus, pressed, selected, busy, optimistic, drag/drop, disclosure, direct-manipulation, or interaction polish that risks feeling cheap, read `microinteraction-affordance-system.md` after `interaction-state-matrix.md`. Use it to define state vocabulary, feedback jobs, timing/easing, pointer/touch parity, selection and drag affordances, async feedback, reduced-motion behavior, library ownership, and QA checks.

For serious usable product surfaces, read `../ux-patterns/task-ergonomics.md` and `../ux-patterns/usability-pattern-matrix.md` before visual styling. Use them to choose the workflow pattern, speed path, error prevention, recovery path, keyboard/touch model, rejected patterns, and evidence tasks so the UI is not only good looking. When polished UI may still be confusing, follow with `../ux-patterns/usability-heuristic-evaluation.md` for heuristic findings, cognitive walkthrough prompts, severity, evidence, first repair, and re-check.

When usability must be proven rather than assumed, read `../ux-patterns/usability-scenario-testing.md`, `../ux-patterns/task-flow-acceptance-harness.md`, and, after render, `../ux-patterns/rendered-usability-acceptance-gate.md` after task ergonomics and pattern selection. Use them to define first-run, returning-user, error/recovery, keyboard-only, mobile/touch, and state-stress tasks with friction budget, selectors/test hooks, rendered evidence, repair, re-check, and accept/repair/block decision.

For serious UI generation, audits, public demos, or design-quality improvement work, read `design-quality-metrics.md` before final delivery. Score relevant dimensions, check hard fail gates, cite evidence, and repair high-impact failures before polish. When product success must be proven beyond screenshots, follow with `ux-measurement-instrumentation-contract.md` to define success metrics, friction/recovery signals, event taxonomy, privacy boundaries, accessibility/performance signals, and a learning loop.

For generated outputs, dogfood batches, README/gallery candidates, and comparisons across drafts, read `generated-output-scorecard.md` after rendered evidence. Use it to require an evidence pack, hard fails, weighted scores, confidence cap, gate result, publish decision, one required repair, re-check evidence, and feedback-memory decision.

After scoring, read `quality-calibration-ladder.md` when the result is below the requested level but not obviously broken. Use it to choose the one repair most likely to move the design up a tier.

When generated examples, screenshot proofs, showcase work, or Stark design improvements need to be tested as a batch, read `dogfood-generation-loop.md` and `dogfood-batch-matrix.md`. Use them to select standard representative surfaces, prompts, evidence, rendered quality gates, repairs, feedback memory entries, guidance updates, and gallery decisions.

When multiple generated designs, README/gallery screenshots, release examples, public demos, or dogfood batches might be published as proof, read `showcase-selection-quality-contract.md` after dogfood, rendered quality, and acceptance gates. Use it to select examples by distinct product/design strength, reject weak or repetitive outputs, choose repair candidates, and report gallery gaps.

When rendered evidence exists for serious, public, portfolio, animation-led, README/gallery, or user-critiqued work, read `rendered-quality-gate.md` before acceptance. Use it to judge the actual screenshot or DOM result: first read, product object clarity, composition geometry, typography texture, proof credibility, state usefulness, asset/material quality, motion still-frame quality, responsive dignity, library/default residue, and screenshot-worthiness.

After a first generated draft renders for high-craft, public, app-like, animation-led, product-proof, or user-critiqued work, read `first-draft-triage-loop.md` before polishing. Use it to decide keep, repair, rebuild, or reject; preserve the strongest decision; identify the weakest blocking decision; choose the next action; and name the evidence to re-check.

When evidence exists but the right repair is unclear, read `design-critique-panel.md` before the repair playbook. Use product, UX, visual composition, typography/copy, motion/media, accessibility, and implementation/library lenses to preserve one strong decision and choose one high-risk weakness to fix.

Before final delivery on serious, public, high-craft, animation-led, "go all out", or user-critiqued UI, read `design-acceptance-gate.md` after QA, metrics, and calibration. Classify the result as blocked, passable, good, high-craft, or portfolio, then continue repair when the gate is below the requested level.

When user critique or repeated QA reveals a design failure that could recur, read `feedback-memory-loop.md` before the next generation. Use it to preserve the user's wording, classify symptom/severity, record evidence, worked/failed repairs, a future ban, a future preference, a stress test, scope, and expiration.

For animation creation, Luma-style motion, scroll stories, cinematic product reveals, or immersive demos, read `animation-creation.md` before `interaction-techniques.md`. Decide the motion thesis, main object, storyboard, assets, libraries, performance budget, and reduced-motion fallback before implementation.

When an animation-led page feels messy, copied, cheap, low-quality, or like pasted props around a reference, read `premium-animation-direction.md` after `animation-creation.md`. Use it to define product truth, main object, signature transformation, visual world, motif, proof surface, copy voice, asset quality plan, originality delta, and premium QA gates before choreography.

When choreography is central, read `choreography-state-machine.md` after `animation-creation.md` and before implementation. Use it to define scenes, triggers, interaction/system states, ownership, timing, persistent controls, reduced-motion substitutions, and QA probes.

When choreography is central, read `motion-timing-easing-system.md` after the state machine so duration, easing, staging, interruption behavior, resting frames, reduced-motion substitution, persistent-motion controls, and performance-safe properties are named before library choice. Then read `motion-pattern-atlas.md` and pick one primary motion pattern such as object birth, scroll proof film, shared-object continuity, material inspection, prompt-to-output trace, cursor lens, state-machine illustration, data morph, kinetic type, 2D canvas field, designer-authored sequence, or view choreography.

When implementation will use Motion, GSAP, Three/R3F, Rive, Lottie, video, Lenis, Anime.js, Theatre.js, PixiJS, View Transitions, Web Animations API, or native scroll CSS, read `motion-library-playbooks.md` after choosing the motion pattern. Define ownership, rejected libraries, fallback, and QA instead of piling on libraries. If the chosen motion depends on authored timelines, keyframes, Rive state machines, Lottie/dotLottie loops, video composites, Spline scenes, Pixi/canvas fields, or SVG path films, follow with `designer-authored-motion-handoff.md` so named beats, runtime controls, labels/inputs, DOM-owned content, exports, fallbacks, mobile/reduced-motion substitutes, versioning, and QA hooks are explicit before implementation.

For serious animation acceptance, read `motion-frame-qa-contract.md` after library choice and media loading. Use it to define poster/start/beat/proof/end/reduced-motion/mobile frames, capture method, stable state hooks, nonblank media probes, visual comparison risk, required repair, and re-check evidence.

When the signature visual depends on shader-like visuals, canvas fields, WebGL, Spline-style embeds, Rive state machines, Lottie loops, video composites, or generated key art, read `advanced-visual-systems.md` after the motion pattern and asset pipeline. Use it to choose one main mechanic, visual owner, motif, inputs, code-native content, fallback, rejected spectacle, and media/canvas QA.

When implementation quality matters, read `frontend-technique-matrix.md` after stack selection and before library selection. Use it to choose Grid/subgrid, container queries, custom properties, typed tokens, native scroll CSS, View Transitions, Popover/anchor positioning, responsive media, and performance containment before reaching for a dependency.

When implementation adds expensive motion, media, WebGL/canvas, data visualization, large lists, generated imagery, or multiple libraries, read `performance-budget-contract.md` before implementation. Use it to define critical path, heavy features, budgets, fallbacks, offscreen/pause behavior, rejected effects, and performance evidence.

When the choreography maps to a common implementation pattern, read the relevant `../motion-recipes/*.md` recipe after `motion-library-playbooks.md`.

Use `design-recipes.md` to turn product categories into concrete starting instincts, and use `anti-default-contrasts.md` whenever the layout becomes sidebar/cards/table, centered hero/three cards, chat/spinner/output, or another familiar generated skeleton.

Do not apply a visual style because it is fashionable. Pick it because it supports the product job, platform, and frequency of use.

For dense products, also decide responsive containment before code: tables, nav rows, toolbars, inspectors, and canvases need bounded scroll, wrapping, or a breakpoint-specific replacement. The page itself should not drift sideways.

When assets matter, add an asset plan before code. Choose whether the design needs icons, product screenshots, generated product mocks, typography, textures, reference UI patterns, brand assets, or GPT/Codex image generation. Pull only what supports the job and store attribution for external sources.

When product proof, hero media, generated art, screenshots, charts, video, Rive/Lottie, or Three/WebGL matter, read `asset-realism-matrix.md` before production. Use it to choose proof versus mood, realism level, source, code-native text/controls, responsive framing, accessibility, performance, rejected asset types, and QA probes.

When hero key art, generated visuals, product screenshots, cinematic proof, or responsive crops carry first-impression quality, read `art-direction-asset-board.md` after asset realism and before production. Use it to define shot role, layer stack, DOM-owned content, generated/image prompt, negative constraints, desktop/tablet/mobile crops, fallback/poster, performance, accessibility, and QA.

When media quality depends on loading, LCP, responsive crops, posters, or runtime fallback, read `media-loading-contract.md` after asset production and performance planning. Use it to define critical media, responsive sources, reserved geometry, loading priority, decode/preload/fetch priority, poster/static fallback, reduced motion, offscreen policy, accessibility text, and nonblank QA.

When visual materials matter, read `material-systems.md` and choose a material language that supports the product world, hierarchy, platform constraints, and accessibility. Avoid trend materials that do not clarify the interface.

For concrete color, spacing, radius, border, shadow, density, semantic state, and material tokens, read `visual-token-recipes.md` after `material-systems.md`. Use it to assign token roles, palette ratio, surface roles, state colors, elevation, contrast, banned defaults, and QA checks. After render, read `color-material-contrast-gate.md` so actual text/non-text contrast, focus, selected/invalid/danger states, chart/media colors, material depth, and default palette residue are accepted, repaired, or blocked with evidence.

For light/dark mode, high contrast, forced colors, compact/comfortable density, brand themes, or platform appearance adaptation, read `theme-mode-density-system.md` after `visual-token-recipes.md`. Use it to preserve semantic token parity, focus/selection visibility, state meaning, media/chart framing, first-paint behavior, and density across modes.

Before serious implementation, read `token-implementation-contract.md` after visual tokens and theme modes. Use it to map primitive, semantic, component, state, mode/density, and chart/media tokens into CSS custom properties, Tailwind/theme variables, product-library overrides, contrast checks, dead-token checks, and QA evidence.

When producing assets, read `asset-production-pipeline.md` after `asset-selection.md`. Choose screenshot, code-rendered mock, generated bitmap, SVG, icon, chart, video, Rive, Lottie, or Three based on the asset job and QA requirements.

When those assets become the main visual language, follow with `art-direction-asset-board.md` and `advanced-visual-systems.md` so the result has produced key art and a product-tied mechanic instead of random spectacle.

When icons or illustrations matter, read `icon-illustration-systems.md`. Use platform icons or lucide where appropriate, keep styles consistent, and make empty-state/diagram art support the next action or proof.

When shipped references matter, add a reference extraction brief before code. Extract structure, interaction, state, recovery, responsive, and asset lessons from real products; do not copy their visuals.

Before final delivery on high-craft, public, product-heavy, or animation-heavy UI, read `qa-evidence-contract.md`, `browser-qa-matrix.md`, `visual-qa-protocol.md`, `visual-qa-workflow.md`, `implementation-review-loop.md`, `runtime-evidence-regression-contract.md`, and `design-acceptance-gate.md`, then verify the result with browser/runtime evidence where possible. Critique the screenshot, compare the implementation to the source decisions, apply at least one obvious fix when present, re-check the failed evidence, and do not stop below the requested quality level.

When a screenshot, browser probe, design score, or user critique reveals a quality problem, read `visual-repair-playbook.md` before polishing. Use it to map the symptom to the likely cause, first repair, evidence to re-check, and what not to do.

When that critique should influence future attempts, follow with `feedback-memory-loop.md` so the next brief carries forward concrete bans, preferences, and stress tests instead of repeating the same failure.
